# Project instructions

- Communicate in Korean by default
- Keep responses concise and practical
- Preserve the existing visual style and animal character identities
- Implement only the requested scope, then run focused verification
- Never commit or print API keys, tokens, .env.local, or other secrets
- Keep OpenAI calls behind the Vercel API routes; never expose keys to client code
- Before release, run npm test, npm run lint, and npm run build:web
- Build the Apps in Toss bundle with npm run build:ait
- Treat giteukhae.ait, dist/, and local screenshots as release artifacts, not source
- Do not request an Apps in Toss review or change billing limits without explicit user approval

